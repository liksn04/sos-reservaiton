# Change Group Plan - 2026-06-03

## Purpose

This document keeps the current dirty worktree from becoming one vague change.
It records review and future commit groups only. No staging, commit, stash,
branch creation, discard, checkout, migration application, or destructive
revert was performed while creating this plan.

## Current Groups

### Group 1: Runtime Font/Login

Purpose:
- Move app typography to local Pretendard.
- Remove the previous external app-font Google stylesheet.
- Preserve Material Symbols.
- Add the `/login` copyright line.

Files:
- `index.html`
- `src/routes/Login.tsx`
- `src/styles/app.css`
- `src/styles/base.css`
- `src/styles/modals.css`
- `src/styles/routes-login.css`
- `tailwind.config.js`
- `src/styles/fonts.css`
- `public/fonts/pretendard/*.otf`

Known notes:
- The local OTF set is about 13M.
- License/source was verified externally during Wave 1.
- A future performance pass can consider WOFF2, variable, or subset delivery.

Suggested commit message if requested:

```text
Use local Pretendard font on login surface
```

### Group 2: AGENTS Meta Guidance

Purpose:
- Preserve repository guidance generated by `omo:init-deep`.
- Keep agent/process guidance separate from runtime app behavior.

Files:
- `AGENTS.md`
- `src/AGENTS.md`
- `src/components/AGENTS.md`
- `src/hooks/AGENTS.md`
- `src/styles/AGENTS.md`
- `src/utils/AGENTS.md`
- `supabase/AGENTS.md`

Suggested commit message if requested:

```text
Add scoped AGENTS guidance for Roomin
```

### Group 3: OMO Process Evidence

Purpose:
- Keep the realignment, review, and execution evidence available as project
  history.
- Keep process artifacts separate from app runtime changes.

Files:
- `.omo/boulder.json`
- `.omo/drafts/roomin-realignment.md`
- `.omo/plans/roomin-realignment.md`
- `.omo/start-work/ledger.jsonl`
- `.omo/start-work/runtime-font-login-browser-qa.json`
- `.omo/start-work/screenshots/*.png`
- `docs/worktree-triage-2026-06-03.md`
- `docs/roadmap-reset-2026-06-03.md`
- `docs/runtime-font-login-closeout-2026-06-03.md`
- `docs/agents-hierarchy-closeout-2026-06-03.md`
- `docs/execution-wave-2-2026-06-03.md`
- `docs/change-group-plan-2026-06-03.md`

Suggested commit message if requested:

```text
Record Roomin realignment evidence
```

### Group 4: Public Route QA And Event Count

Purpose:
- Add repeatable public-route browser QA.
- Implement the product decision that ordinary approved users can see event
  participant counts.
- Keep participant names, roster details, and attendance state admin-only.

Files:
- `package.json`
- `scripts/verify-public-routes.mjs`
- `src/hooks/useEventParticipantSummaries.ts`
- `src/utils/eventParticipantSummary.ts`
- `src/utils/eventParticipantSummary.test.ts`
- `src/components/EventParticipantsModal.tsx`
- `src/lib/RealtimeProvider.tsx`
- `supabase/migrations/20260603190000_publish_event_participant_counts_realtime.sql`

Suggested commit message if requested:

```text
Expose event participant counts to approved users
```

## Supabase Migration Status

The migration file is prepared but not applied:

```text
supabase/migrations/20260603190000_publish_event_participant_counts_realtime.sql
```

Recommended order if migration application is later requested:
1. Apply to local or staging Supabase first.
2. Confirm `event_participant_counts` is already present from
   `20260419164222_structural_drift_cleanup.sql`.
3. Apply the Realtime publication migration.
4. Only then apply to production.

## Verification Already Run

Passed:

```text
npm run lint
npm run test
npm run build
npm run check:bundle
npm run qa:public-routes
npm audit --audit-level=high
```

The latest code-only check also passed:

```text
npm run lint
npm run test
```

## Next Safe Action

If the user asks for commits, stage and commit one group at a time in the order
above. Do not mix Group 1 runtime changes with Group 3 OMO evidence.
